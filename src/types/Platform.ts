/**
 * This enum holds the different types of platforms which bStats supports
 * at the moment.
 */
export const enum Platform {

    BUKKIT = 'bukkit',
    BUNGEECOORD = 'bungeecoord',
    SPONGE = 'sponge',
    SERVER_IMPLEMENTATION = 'server-implementation',
    OTHER = 'other'

}
